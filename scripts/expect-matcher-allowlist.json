{
  "_comment": [
    "FAIL-CLOSED FALLBACK for the expect-matcher gate (scripts/check-matchers.mjs).",
    "SOURCE OF TRUTH is the runner shim's SUPPORTED_MATCHERS array",
    "(synthwatch@main runner/specfetch/specShim.ts) — CI fetches that file and parses it",
    "live (SHIM_SOURCE). This committed snapshot is used ONLY when the fetch or parse",
    "fails, with a loud warning; it is restrictive (never allow-all) and may lag the shim.",
    "Snapshot of the shim's implemented set as of 2026-07-04 (11 matchers, runner recon).",
    "Keep in sync opportunistically when the shim adds a matcher."
  ],
  "allowed": [
    "toBe",
    "toBeDefined",
    "toBeFalsy",
    "toBeGreaterThan",
    "toBeGreaterThanOrEqual",
    "toBeLessThan",
    "toBeLessThanOrEqual",
    "toBeNull",
    "toBeTruthy",
    "toBeVisible",
    "toHaveURL"
  ]
}
