name: TEMP prove-can-fail (delete me)
# Deliberately carries the base-branch filter this PR's guard forbids, to prove the guard RUNS and REDs
# on a PR whose base is NOT main. Reverted in the next commit.
on:
  pull_request:
    branches: [main]
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: 'true'
